# 2026-03-27

## Models
- User added `minimax-portal/MiniMax-M2.7` as a fallback LLM model.
- API key configured in `openclaw.json` env vars and `.env.d/minimax-api-key.txt`.
- Provider config added to `models.providers.minimax-portal` with Anthropic-compatible API at `https://api.minimax.io/anthropic`.
- Auth profile `minimax-portal:default` added.
- Gateway restarted, model confirmed loaded as fallback #1.

## Multi-Agent Architektur (wichtige Entscheidungen)

### ChatGPT Recherche-Ergebnis
- OpenClaw hat Multi-Agent, Sub-Agents und Agent-Koordination **First-Class**
- **Wichtigste Erkenntnis:** NICHT forken! Stattdessen:
  - Plugins/Hooks für neue Capabilities
  - Separate UI (spricht über WS/HTTP mit Gateway)
  - Model-Policy pro Agent
- Dashboard: Separate UI statt Fork (Option C)
  - Gateway WebSocket Protocol + HTTP APIs nutzen
  - TenacitOS als Referenz für Features
  - Repo: `rook-dashboard` (eigenes Projekt)
- Self-Improvement nur über PR-Pfad (Agent erstellt Branches, kein Direct-Deploy)
- Rescue Gateway als Backup-Instanz

### Architektur-Dokument erstellt
- Pfad: `engineering/docs/MULTI-AGENT-ARCHITECTURE.md` (v2)
- Agent-Portfolio: Rook (Main), Coach, Engineer, Researcher, Health, Consultant

### Dateien wiederhergestellt
- SOUL.md und USER.md waren mit leeren Templates überschrieben (bei Reorganisation)
- Aus Git-History wiederhergestellt (Commit 103f47b)
- Beide Repos (rook-agent + rook-workspace) gepusht

## Consultant-Agent erstellt
- `openclaw agents add consultant` → erfolgreich
- Workspace: `/root/.openclaw/workspace-consultant/`
- Agent dir: `/root/.openclaw/agents/consultant/agent`
- Enthält Default-Templates (SOUL.md, USER.md, AGENTS.md, etc.)
- **Noch zu tun:** 
  - SOUL.md für IT-Consulting bei HiSolutions anpassen
  - USER.md mit Marcus' Berufsprofil füllen
  - Bindings einrichten für Telegram-Routing
  - Testen

## Offene Tasks
- [ ] Consultant-Agent konfigurieren (SOUL.md, USER.md, AGENTS.md)
- [ ] Bindings für Agent-Routing einrichten
- [ ] Dashboard-Repo erstellen (rook-dashboard)
- [ ] Gateway Protocol studieren
- [ ] Model-Fallbacks konfigurieren (OpenAI als Backup)
